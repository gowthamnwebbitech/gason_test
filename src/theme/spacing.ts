import {
  moderateScale,
} from './responsive';

export const spacing = {
  none: 0,

  xs: moderateScale(4),

  sm: moderateScale(8),

  md: moderateScale(16),

  lg: moderateScale(24),

  xl: moderateScale(32),

  xxl: moderateScale(40),

  xxxl: moderateScale(48),
};